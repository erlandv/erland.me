{
  "name": "erland.me",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "predev": "npm run copy:favicon && npm run generate:robots && npm run generate:ads && npm run generate:search",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run copy:favicon && npm run generate:robots && npm run generate:ads && npm run generate:search && AVIF=false astro build && npm run generate:sitemap",
    "build:dev": "PUBLIC_SITE_ENV=development npm run copy:favicon && npm run generate:robots && npm run generate:ads && npm run generate:search && astro build && npm run generate:sitemap",
    "build:clean": "rm -rf dist && npm run copy:favicon && npm run generate:robots && npm run generate:ads && npm run generate:search && AVIF=false astro build && npm run generate:sitemap",
    "preview": "astro preview",
    "astro": "astro",
    "lint": "astro check",
    "lint:fix": "astro check --fix",
    "lint:js": "eslint .",
    "lint:js:fix": "eslint . --fix",
    "type-check": "astro check --only=types",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate": "npm run lint && npm run lint:js && npm run type-check && npm run format:check",
    "generate:robots": "node scripts/generate-robots.mjs",
    "generate:ads": "node scripts/generate-ads.mjs",
    "generate:search": "node scripts/generate-search-index.mjs",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "copy:favicon": "cp public/assets/favicon/favicon.ico public/favicon.ico"
  },
  "engines": {
    "node": ">=22.18.0",
    "npm": ">=10.9.0"
  },
  "dependencies": {
    "@playform/compress": "^0.2.0",
    "@writenex/astro": "^0.2.2",
    "astro": "^5.15.1",
    "fuse.js": "^7.1.0",
    "gray-matter": "^4.0.3",
    "sharp": "^0.34.4",
    "web-vitals": "^5.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.5",
    "@types/node": "^24.5.2",
    "@typescript-eslint/eslint-plugin": "^8.46.4",
    "@typescript-eslint/parser": "^8.46.4",
    "autoprefixer": "^10.4.21",
    "eslint": "^9.39.1",
    "eslint-plugin-astro": "^1.5.0",
    "jiti": "^2.6.1",
    "prettier": "^3.6.2",
    "prettier-plugin-astro": "^0.14.1",
    "remark-directive": "^4.0.0",
    "typescript": "^5.9.2",
    "unist-util-visit": "^5.0.0"
  }
}
