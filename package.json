{
  "name": "erland.me",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run generate:robots && astro build",
    "build:clean": "rm -rf dist && npm run generate:robots && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "lint": "astro check",
    "lint:fix": "astro check --fix",
    "type-check": "astro check --only=types",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate": "npm run lint && npm run type-check && npm run format:check",
    "generate:robots": "node scripts/generate-robots.js"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.1.5",
    "remark-directive": "^4.0.0",
    "typescript": "^5.9.2",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "@types/node": "^24.5.2",
    "autoprefixer": "^10.4.21",
    "cssnano": "^7.1.1",
    "html-minifier-terser": "^7.2.0",
    "prettier": "^3.6.2",
    "terser": "^5.44.0"
  }
}
