---
import ErrorPage from '@components/ErrorPage.astro';
import Icon from '@components/Icon.astro';
import { PAGE_METADATA } from '@lib/content/page-metadata';
---

<ErrorPage
  errorCode='404'
  errorTitle='Page Not Found'
  errorMessage="You've reached a place that absolutely shouldn't exist. I'd tell you how you got here, but honestly… I have no idea. Let's head back before reality collapses."
  title={PAGE_METADATA.notFound.title}
  description={PAGE_METADATA.notFound.description}
>
  <Icon slot='icon' name='error404' />
</ErrorPage>
